mixin category(category)
    
        li.list-group-item.mb-2(id=category.name) #[i(class=category.icon)] #{category.name}

mixin command(command)
        li.list-group-item.mb-2(class=command.category, id=command.name) #{command.name}